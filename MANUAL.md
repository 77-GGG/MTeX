# MTeX 使用手册

MTeX 是一款跨平台桌面笔记本应用，支持 **Markdown** 和 **LaTeX** 双格式编辑，具备强大的全文搜索与笔记索引功能。

---

## 目录

1. [快速开始](#1-快速开始)
2. [界面布局](#2-界面布局)
3. [文件管理](#3-文件管理)
4. [模板系统](#4-模板系统)
5. [编辑笔记](#5-编辑笔记)
6. [Markdown 预览](#6-markdown-预览)
7. [LaTeX 编译](#7-latex-编译)
8. [搜索功能](#8-搜索功能)
9. [标签系统](#9-标签系统)
10. [收藏功能](#10-收藏功能)
11. [命令面板](#11-命令面板)
12. [快捷键速查](#12-快捷键速查)

---

## 1. 快速开始

### 安装与启动

```bash
cd /Users/qiguo/Documents/MTeX
npm run dev
```

### 打开工作区

1. 启动后点击 **Open Workspace Folder**
2. 选择一个文件夹作为笔记本根目录
3. 所有 `.md` 和 `.tex` 文件会自动显示在左侧文件树

> 💡 也可以用 `Cmd+O` 快捷键打开工作区。

---

## 2. 界面布局

MTeX 采用**三栏固定布局**：

```
┌──────────┬──────────────┬──────────────┐
│  文件栏   │   源代码栏    │   渲染栏     │
│  标签栏   │  CodeMirror  │  预览/PDF   │
│  搜索框   │   语法高亮    │   实时渲染   │
│          │   自动保存    │              │
│← 260px →│←  flex-1  →│←  flex-1  →│
└──────────┴──────────────┴──────────────┘
```

- **文件栏**：文件树 + 标签列表 + 搜索框
- **源代码栏**：CodeMirror 6 编辑器，支持 Markdown/LaTeX 语法高亮
- **渲染栏**：Markdown 实时预览 / LaTeX 编译后的 PDF

三栏各自独立滚动，互不影响。

---

## 3. 文件管理

### 新建笔记

- 点击侧边栏底部的 **"+ .md"** 或 **"+ .tex"** 按钮
- 或使用快捷键 `Cmd+N`（新建 .md）

### 右键菜单

在文件树的任意文件或文件夹上**右键点击**，可以：

| 操作 | 说明 |
|------|------|
| New Markdown Note | 新建 .md 文件 |
| New LaTeX Document | 新建 .tex 文件 |
| New Folder | 新建文件夹 |
| Rename | 重命名 |
| Delete | 删除（有确认提示） |
| Reveal in Finder | 在 Finder 中打开文件位置 |

### 自动保存

编辑内容后 **2 秒自动保存**。工具栏右侧显示保存状态：
- 🟢 **Saved** — 已保存
- 🟡 **Saving...** — 保存中
- 🔴 **Unsaved** — 未保存

### 外部修改检测

如果用其他编辑器（如 VS Code）修改了笔记文件，MTeX 会**自动检测并重新加载**内容。

---

## 4. 模板系统

创建新文件时，MTeX 会弹出模板选择器，可以选择预设模板或从空白开始。

### 内置模板

| Markdown | LaTeX |
|----------|-------|
| 空白笔记 | 基础文章（article） |
| 会议笔记 | 中文文章（ctex） |
| 日记 | Beamer 演示 |
| 研究笔记 | |

### 选择模板

1. 点击侧边栏 **"+ .md"** 或 **"+ .tex"**
2. 弹出模板选择器，左侧是模板列表
3. **单击**模板名 → 右侧预览模板内容
4. **双击**模板名 → 直接选中并创建
5. 点击 **"Skip (Blank)"** → 不选模板，创建空白文件
6. 点击 **"Use xxx"** → 用选中的模板创建文件

> 💡 模板中的 `{{date}}` 占位符会在创建时自动替换为当天日期。

### 图标说明

| 图标 | 含义 |
|------|------|
| 📦 | 内置模板（系统自带，不可删除） |
| 👤 | 用户模板（你自己创建的） |

### 创建自定义模板

**方式一：保存当前笔记为模板**

1. 打开一篇已编辑好的笔记
2. 点击 **"+ .md"** 或 **"+ .tex"** 打开模板选择器
3. 点击顶部的 **💾 Save Current as Template** 按钮
4. 输入模板名称 → 回车保存

**方式二：手动创建文件**

在 `~/Library/Application Support/mtex/templates/` 目录下创建模板文件：

```
~/Library/Application Support/mtex/templates/
├── md/
│   └── 我的模板.md
└── tex/
    └── 我的模板.tex
```

### 删除自定义模板

模板选择器中，自定义模板（👤 图标）右侧悬停出现 **×** 按钮，点击删除。

> ⚠️ 内置模板（📦 图标）不可删除。

### 模板优先级

如果自定义模板与内置模板同名，**优先使用自定义模板**，方便覆盖内置预设。

---

## 5. 编辑笔记

### Markdown 语法

编辑器支持完整 Markdown 语法：

```markdown
# 标题
## 二级标题

**粗体** *斜体* ~~删除线~~

- 列表项
- 列表项

1. 有序列表
2. 有序列表

> 引用块

`行内代码`

​```python
# 代码块（带语法高亮）
print("Hello MTeX")
​```

[链接](https://example.com)

| 表格 | 标题 |
|------|------|
| 数据 | 数据 |
```

### LaTeX 数学公式

在 Markdown 中嵌入数学公式：

```markdown
行内公式：$E = mc^2$

块级公式：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
```

### Wikilink 双向链接

使用 `[[文件名]]` 链接到其他笔记：

```markdown
参考 [[另一篇笔记]]
或者 [[笔记名|显示别名]]
```

点击紫色虚线下划线的链接即可跳转。

### LaTeX 文档

`.tex` 文件使用 LaTeX 语法：

```latex
\documentclass{article}
\begin{document}

\title{我的论文}
\author{作者名}
\maketitle

\section{引言}
这是引言内容...

\end{document}
```

---

## 6. Markdown 预览

### 实时预览

打开 `.md` 文件时，右侧渲染栏自动显示渲染后的 HTML。支持：

- ✅ 标题、段落、列表
- ✅ 表格（GFM）
- ✅ 代码块（语法高亮）
- ✅ 引用块
- ✅ 数学公式（KaTeX 渲染）
- ✅ Wikilink 链接
- ✅ 深色/浅色模式自动适配

### 保存为 PDF

渲染栏右上角点击 **📄 Save PDF** 按钮，将当前 Markdown 笔记导出为 PDF 文件。

---

## 7. LaTeX 编译

### 编译 PDF

1. 打开 `.tex` 文件
2. 渲染栏顶部出现 **▶ Compile** 按钮
3. 点击编译（需要安装 MacTeX 或 TeX Live）
4. 编译成功后，PDF 显示在渲染栏
5. 编译失败时，红色区域显示错误详情

### 安装 LaTeX

```bash
# macOS 推荐安装 MacTeX
brew install --cask mactex
# 或从官网下载：https://tug.org/mactex/
```

### 保存 PDF

编译成功后，点击 **📄 Save PDF** 下载 PDF 文件。

---

## 8. 搜索功能

### 快速搜索（侧边栏）

在文件栏的搜索框中输入关键词，实时下拉显示匹配结果：

- 支持**中文搜索**（模糊匹配）
- 支持**英文搜索**（FTS5 全文索引 + 相关性排序）
- 结果高亮显示搜索词
- `↑↓` 方向键选择，`Enter` 打开，`Esc` 关闭

### 全文搜索面板（Cmd+Shift+F）

按 `Cmd+Shift+F` 打开全功能搜索面板：

| 过滤条件 | 说明 |
|----------|------|
| **格式过滤** | All / .md / .tex |
| **标签过滤** | 多选标签筛选 |
| **键盘导航** | `↑↓` 切换 · `Enter` 打开 · `Esc` 关闭 |

### 搜索词高亮

从搜索结果打开文件后，编辑器中所有匹配的搜索词会**临时高亮 4 秒**，方便快速定位。

---

## 9. 标签系统

### 创建标签

1. 侧边栏底部 **Tags** 旁点 **+**
2. 输入标签名 → 回车
3. 系统随机分配颜色

### 给笔记打标签

1. 打开一篇笔记
2. 工具栏点击 **"+ tag"**
3. 下拉选择已有标签
4. 标签显示为彩色 chip

### 移除标签

点击标签 chip 右侧的 **×**

### 按标签筛选

- 侧边栏 **点击标签** → 文件树只显示打了该标签的笔记
- 顶部出现 **Filtered ×** → 点击取消筛选
- 标签和收藏筛选可以叠加

### 标签存储

标签自动写入 `.md` 文件的 YAML frontmatter：

```yaml
---
tags:
  - 数学
  - 笔记
---
```

同时在 SQLite 数据库中索引，确保搜索效率。

---

## 10. 收藏功能

### 收藏笔记

- 编辑器工具栏点 **☆** → 变成黄色 **★** 表示已收藏
- 文件树中收藏的文件右侧显示 **★**

### 筛选收藏

两种方式：
1. 侧边栏顶部点 **★** 按钮
2. TagList 中点击 **★ 收藏** 条目

文件树只显示收藏的笔记。

---

## 11. 命令面板

按 `Cmd+P` 打开命令面板，支持：

| 功能 | 说明 |
|------|------|
| **快速打开文件** | 输入文件名 → 直接跳转 |
| **New Markdown Note** | 新建 .md 笔记 |
| **New LaTeX Document** | 新建 .tex 文档 |
| **Toggle Bookmark** | 切换当前笔记收藏 |
| **Search Notes...** | 打开搜索面板 |
| **Toggle Dark/Light Theme** | 切换深色/浅色主题 |

操作：`↑↓` 选择 · `Enter` 执行 · `Esc` 关闭

---

## 12. 快捷键速查

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd+P` | 命令面板 |
| `Cmd+Shift+F` | 全文搜索面板 |
| `Cmd+N` | 新建 Markdown 笔记 |
| `Cmd+O` | 打开工作区文件夹 |
| `Cmd+S` | 保存当前文件 |
| `Cmd+R` | 刷新窗口 |
| `Cmd+Option+I` | 开发者工具 |

### 编辑器快捷键

CodeMirror 6 标准快捷键（部分）：

| 快捷键 | 功能 |
|--------|------|
| `Cmd+Z` | 撤销 |
| `Cmd+Shift+Z` | 重做 |
| `Cmd+X` / `Cmd+C` / `Cmd+V` | 剪切 / 复制 / 粘贴 |
| `Cmd+A` | 全选 |
| `Cmd+F` | 在文件内搜索 |

### 搜索面板快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑` / `↓` | 切换搜索结果 |
| `Enter` | 打开选中结果 |
| `Esc` | 关闭面板 |

---

## 技术信息

| 项目 | 技术 |
|------|------|
| 桌面框架 | Electron 34 |
| 前端 | React 19 + TypeScript 5.7 |
| 样式 | Tailwind CSS 4 |
| 编辑器 | CodeMirror 6 |
| 数据库 | SQLite + FTS5 |
| Markdown | markdown-it + KaTeX + highlight.js |
| LaTeX | pdfTeX + 自定义协议 PDF 预览 |
| 构建 | Vite 6 |

---

## 文件结构

```
工作区/
├── 笔记1.md
├── 笔记2.tex
├── 子文件夹/
│   └── 笔记3.md
└── ...
```

- `.md` 文件：Markdown 格式
- `.tex` 文件：LaTeX 格式
- 支持嵌套文件夹
- 隐藏文件（`.` 开头）和符号链接自动忽略
